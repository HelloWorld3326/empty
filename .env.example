# 复制为 .env。这个文件不要提交到仓库。

# 阿里云百炼 API Key
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxx

# 网关鉴权用的共享 token。留空则不校验（仅限本地开发）。
AGENTBASE_API_TOKEN=

# 生产环境必须设置，它会阻止本地沙箱被误用
# AGENTBASE_ENV=production

# 业务库只读账号
CRM_DB_HOST=10.0.0.10
CRM_DB_USER=readonly_agent
CRM_DB_PASSWORD=
